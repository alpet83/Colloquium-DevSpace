# Дополнение к корневому .editorconfig: сценарии здесь попадают в образ через COPY (байты как в рабочем дереве).
root = false

[*.sh]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
